{
  "name": "nodejs-server",
  "version": "1.0.0",
  "description": "A Node.js API deployed on Vercel",
  "main": "api/get-access-token.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step defined'",
    "start": "node api/get-access-token.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "vercel": "^28.0.0"
  }
}
